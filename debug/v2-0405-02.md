1. ~~项目详情页，概览这个页面，白色背景下整体颜色不对~~ **[已修复]** — 概览页改为 light/dark 双主题适配（slate 色系 + dark: 前缀）

2. ~~资产页面，白色背景下，表格里灰色颜色太灰了~~ **[已修复]** — 三个资产表格（域名/主机/Web）统一替换为 slate-500/dark:slate-400

3. ~~漏洞有重复，需要去重，另外需要按危害进行排序~~ **[已修复]** — findings 页按 severity rank 排序（critical→info）；finding-repo 增加标准化标题模糊去重

4. ~~执行控制tab，说未配置规划模型，但是在全局中已经配置了~~ **[已修复]** — operations 和 ai-logs 页查找 profile role 从 "orchestrator" 改为 "planner"

5. ~~AI日志子tab报错：Cannot read properties of undefined (reading 'length')~~ **[已修复]** — API 返回格式从裸数组改为 `{ items: logs }` 匹配客户端期望

---

## 平台 Bug（从 v2-0405-01 Bug 5 迁移）

6. ~~P0: Node.js IPv6 连接超时~~ **[已修复]** — 新增 instrumentation.ts，在服务启动时设置 `dns.setDefaultResultOrder("ipv4first")`

7. ~~P0: dirsearch_recursive 频繁超时卡死~~ **[已修复]** — stdio-connector RPC timeout 时 SIGKILL 强杀子进程

8. ~~P1: DB 连接池耗尽~~ **[已修复]** — PrismaPg 配置 `max: 10, idleTimeoutMillis: 30000`

9. ~~P1: round_completed 不自动触发~~ **[已修复]** — mcp-run-repo updateStatus 转 terminal 状态时自动检查并发布 round_completed

10. ~~P2: 重复 findings~~ **[已修复]** — finding-repo 增加 normalizeTitle 模糊匹配去重（去除空格/标点/通用词差异）

11. ~~P2: 工具调用参数错误~~ **[已修复]** — execution-worker 增加 rawRequest 参数自动构造；planner prompt 说明 action 字段为自然语言描述

12. P3: httpx/fscan 二进制缺失 — **不修复**（已有优雅错误处理：spawn 失败返回 isError:true，不影响流程）
