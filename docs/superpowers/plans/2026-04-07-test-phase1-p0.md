# Phase 1 (P0): 测试助手 + MCP 层 + LLM 层 + Middleware + API 路由

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全 P0 模块测试：4 个测试助手 + 2 个 MCP 测试 + 4 个 LLM 测试 + 1 个 middleware 测试 + 12 个 API 路由测试，共 ~23 个文件 ~120 cases。

**Architecture:** Unit 测试用 `vi.mock` 隔离依赖；Integration 测试用 PGlite 内存数据库 + msw 拦截 HTTP。API 路由测试直接调用 route handler（传 NextRequest），不经过 middleware（auth 单独测）。

**Tech Stack:** Vitest + vi.mock + msw + PGlite

**依赖:** 需先安装 `msw` devDependency

---

## Task 1: 安装依赖

- [ ] `npm install -D msw`

---

## Task 2: 测试助手 — mock-child-process.ts

**Files:**
- Create: `tests/helpers/mock-child-process.ts`

- [ ] **创建 mock ChildProcess 工厂**

提供可控的 stdin/stdout/stderr 流，用于 stdio-connector 测试。

```typescript
import { EventEmitter, PassThrough } from "stream"

export function createMockChildProcess() {
  const stdin = new PassThrough()
  const stdout = new PassThrough()
  const stderr = new PassThrough()
  const proc = Object.assign(new EventEmitter(), {
    stdin, stdout, stderr,
    pid: 12345,
    killed: false,
    kill(signal?: string) {
      this.killed = true
      this.emit("exit", signal === "SIGKILL" ? 137 : 0, signal)
    },
  })
  return { proc, stdin, stdout, stderr }
}
```

---

## Task 3: 测试助手 — msw-handlers.ts

**Files:**
- Create: `tests/helpers/msw-handlers.ts`

- [ ] **创建 OpenAI API msw handler 集合**

```typescript
import { http, HttpResponse, delay } from "msw"

const BASE = "https://api.openai.test/v1"

// 正常 chat 响应
export function chatOk(content: string, model = "gpt-4") {
  return http.post(`${BASE}/chat/completions`, () =>
    HttpResponse.json({
      choices: [{ message: { content } }],
      model,
      usage: { prompt_tokens: 10, completion_tokens: 20 },
    }))
}

// Function call 响应
export function chatFunctionCall(name: string, args: string) {
  return http.post(`${BASE}/chat/completions`, () =>
    HttpResponse.json({
      choices: [{ message: { content: null, tool_calls: [{
        id: "call_1", type: "function",
        function: { name, arguments: args },
      }] } }],
      model: "gpt-4",
    }))
}

// 429 Rate Limit
export function chat429() {
  return http.post(`${BASE}/chat/completions`, () =>
    new HttpResponse("Rate limited", {
      status: 429,
      headers: { "retry-after": "5" },
    }))
}

// 500 Server Error
export function chat500() {
  return http.post(`${BASE}/chat/completions`, () =>
    new HttpResponse("Internal Server Error", { status: 500 }))
}

// 延迟超时
export function chatSlow(ms: number) {
  return http.post(`${BASE}/chat/completions`, async () => {
    await delay(ms)
    return HttpResponse.json({ choices: [{ message: { content: "late" } }] })
  })
}

// 畸形 JSON
export function chatMalformed() {
  return http.post(`${BASE}/chat/completions`, () =>
    new HttpResponse("{invalid json", { headers: { "content-type": "application/json" } }))
}
```

---

## Task 4: 测试助手 — route-test-utils.ts

**Files:**
- Create: `tests/helpers/route-test-utils.ts`

- [ ] **创建 NextRequest 构造器 + auth token 注入**

```typescript
import { NextRequest } from "next/server"
import { signToken } from "@/lib/infra/auth"

// 构造带 auth cookie 的 NextRequest
export async function createAuthRequest(
  url: string,
  options: RequestInit & { userId?: string; account?: string; role?: string } = {},
) {
  const { userId = "user-1", account = "researcher", role = "researcher", ...init } = options
  const token = await signToken({ userId, account, role })
  const req = new NextRequest(new URL(url, "http://localhost:3000"), init)
  req.cookies.set("pentest_token", token)
  return req
}

// 无 auth cookie 的 NextRequest
export function createAnonRequest(url: string, options: RequestInit = {}) {
  return new NextRequest(new URL(url, "http://localhost:3000"), options)
}

// 构造 route ctx（params）
export function routeCtx(params: Record<string, string>) {
  return { params: Promise.resolve(params) }
}
```

---

## Task 5: 测试助手 — mock-event-source.ts

**Files:**
- Create: `tests/helpers/mock-event-source.ts`

- [ ] **创建 EventSource mock**（Phase 2 hooks 测试用，这里先建好）

```typescript
export class MockEventSource {
  onopen: (() => void) | null = null
  onmessage: ((e: { data: string }) => void) | null = null
  onerror: ((e: unknown) => void) | null = null
  readyState = 0
  close() { this.readyState = 2 }
  // 测试触发器
  triggerOpen() { this.readyState = 1; this.onopen?.() }
  triggerMessage(data: string) { this.onmessage?.({ data }) }
  triggerError(err: unknown) { this.onerror?.(err) }
}
```

---

## Task 6: MCP — stdio-connector.test.ts

**Files:**
- Create: `tests/unit/mcp/stdio-connector.test.ts`
- Test: `lib/mcp/stdio-connector.ts`

- [ ] **Step 1: Mock child_process.spawn**

```typescript
import { vi, describe, it, expect, beforeEach } from "vitest"
import { createMockChildProcess } from "../../helpers/mock-child-process"

const mockSpawn = vi.fn()
vi.mock("child_process", () => ({ spawn: mockSpawn }))
```

- [ ] **Step 2: 实现 7 个测试用例**

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 正常 JSON-RPC 通信 | callTool → stdout 返回 `{jsonrpc:"2.0",id:1,result:{content:[{type:"text",text:"ok"}]}}` → resolve `{content:"ok",isError:false}` |
| 2 | 并发请求 ID 隔离 | 同时调 callTool 3 次 → 分别在 stdout 写回不同 id 的响应 → 各自 resolve 正确值 |
| 3 | 请求超时 | 构造 `timeoutMs: 50`，不写回响应 → reject 包含 "timeout" → `proc.kill` 被调用 |
| 4 | stdin.write 失败 | mock `stdin.write` 第二参数 callback 传入 error → reject + proc.kill |
| 5 | 进程异常退出 | `proc.emit("exit", 1)` → 所有 pending reject "exited" |
| 6 | stderr 消费 | 往 stderr 写数据 → 不抛异常（console.error 被调用） |
| 7 | close() 清理 | 调用 close() → proc.kill("SIGTERM") 被调用 |

关键：每个测试中 `mockSpawn.mockReturnValue(proc)`，用 `stdout.write(JSON.stringify(response) + "\n")` 模拟 MCP 响应。

---

## Task 7: MCP — registry.test.ts

**Files:**
- Create: `tests/unit/mcp/registry.test.ts`
- Test: `lib/mcp/registry.ts`

- [ ] **Step 1: Mock 依赖**

```typescript
const mockConnector = {
  callTool: vi.fn().mockResolvedValue({ content: "ok", isError: false, durationMs: 10 }),
  listTools: vi.fn().mockResolvedValue([]),
  close: vi.fn().mockResolvedValue(undefined),
}
vi.mock("@/lib/mcp/stdio-connector", () => ({
  createStdioConnector: vi.fn(() => mockConnector),
}))
vi.mock("@/lib/repositories/mcp-tool-repo", () => ({
  findAllServers: vi.fn(),
  findByToolName: vi.fn(),
  upsert: vi.fn(),
}))
```

- [ ] **Step 2: 实现 7 个测试用例**

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 首次 getConnector（通过 callTool 触发） | `createStdioConnector` 调用 1 次 |
| 2 | 重复调用命中缓存 | `createStdioConnector` 仍只调用 1 次 |
| 3 | 并发 getConnector 去重 | `Promise.all([callTool, callTool])` → 只 spawn 一次 |
| 4 | server 禁用 → 缓存淘汰 | 先调用成功，然后 `findAllServers` 返回 `enabled:false` → callTool 返回 isError + connector.close 被调用 |
| 5 | callTool 路由正确 | `findByToolName` 返回 `{serverName:"script"}` → connector.callTool 被调用 |
| 6 | callTool 工具不存在 | `findByToolName` 返回 null → `{isError:true, content:"not found"}` |
| 7 | closeAll 清理 | 调用 closeAll() → 所有 connector.close 被调用 |

注意：每个测试前用 `beforeEach` 清理模块级缓存（通过 closeAll）。

---

## Task 8: LLM — openai-provider.test.ts (integration + msw)

**Files:**
- Create: `tests/integration/llm/openai-provider.test.ts`
- Test: `lib/llm/openai-provider.ts`

- [ ] **Step 1: msw server 设置**

```typescript
import { setupServer } from "msw/node"
import { createOpenAIProvider } from "@/lib/llm/openai-provider"
import * as handlers from "../../helpers/msw-handlers"

const server = setupServer()
beforeAll(() => server.listen({ onUnhandledRequest: "error" }))
afterEach(() => server.resetHandlers())
afterAll(() => server.close())

const provider = createOpenAIProvider({
  apiKey: "test-key",
  baseUrl: "https://api.openai.test",
  model: "gpt-4",
  defaultTimeoutMs: 5000,
})
```

- [ ] **Step 2: 实现 8 个测试用例**

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 普通 chat | `server.use(handlers.chatOk("hello"))` → `response.content === "hello"` + `durationMs > 0` |
| 2 | Function Calling | `server.use(handlers.chatFunctionCall("scan", '{"t":"1"}'))` → `response.functionCall.name === "scan"` |
| 3 | 自定义 baseUrl | 构造 `baseUrl: "https://custom.test/v1"` → msw handler 拦截 `custom.test/v1/chat/completions` |
| 4 | 超时 | `server.use(handlers.chatSlow(10000))` + `timeoutMs: 50` → reject 包含 "abort" |
| 5 | 429 Rate Limit | `server.use(handlers.chat429())` → reject 包含 "429" |
| 6 | 500 Server Error | `server.use(handlers.chat500())` → reject 包含 "500" |
| 7 | 畸形 JSON | `server.use(handlers.chatMalformed())` → reject（不崩溃） |
| 8 | AbortSignal | `AbortController.abort()` 后调用 → reject "aborted" |

---

## Task 9: LLM — react-prompt.test.ts

**Files:**
- Create: `tests/unit/llm/react-prompt.test.ts`
- Test: `lib/llm/react-prompt.ts`

- [ ] **Mock system-prompt**

```typescript
vi.mock("@/lib/llm/system-prompt", () => ({
  loadSystemPrompt: vi.fn().mockResolvedValue("你是安全评估AI"),
}))
```

- [ ] **实现 5 个测试用例**

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 基本构建 | 传入完整 ReactContext → 返回字符串包含项目名、阶段名、目标列表 |
| 2 | 工具列表注入 | `availableTools: [{name:"scan",description:"扫描"}]` → 输出包含 `**scan**: 扫描` |
| 3 | 无工具场景 | `availableTools: []` → 不崩溃，不包含"可用工具"段落 |
| 4 | 阶段上下文 | `currentPhase: "assessment"` → 输出包含该阶段标签 |
| 5 | 空资产/发现 | assets=[], findings=[] → 包含"尚无"提示 |

---

## Task 10: LLM — system-prompt.test.ts

**Files:**
- Create: `tests/unit/llm/system-prompt.test.ts`
- Test: `lib/llm/system-prompt.ts`

- [ ] **实现 2 个测试用例**

```typescript
vi.mock("fs/promises", () => ({
  readFile: vi.fn(),
}))
```

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 文件存在 → 加载内容 | mock readFile 返回模板 → `loadSystemPrompt()` 返回该内容 |
| 2 | 文件不存在 → fallback | mock readFile 抛出 → 返回 fallback（包含"安全评估"） |

注意：每个测试前调用 `invalidatePromptCache()` 清除缓存。

---

## Task 11: LLM — prompts.test.ts

**Files:**
- Create: `tests/unit/llm/prompts.test.ts`
- Test: `lib/llm/prompts.ts`

- [ ] **实现 5 个测试用例**

```typescript
vi.mock("@/lib/llm/system-prompt", () => ({
  loadSystemPrompt: vi.fn().mockResolvedValue("你是安全评估AI"),
}))
```

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | buildAnalyzerPrompt | 传入 AnalyzerContext → messages[0].role=system, messages[1].role=user, 包含 toolName 和 rawOutput |
| 2 | buildReviewerPrompt | 传入 ReviewerContext → user content 包含 roundSummary 和 phase |
| 3 | buildVerifierPrompt | 传入 VerifierContext → user content 包含 finding.title |
| 4 | parseLlmJson — 正常 + markdown fence | `parseLlmJson('```json\n{"a":1}\n```')` → `{a:1}` |
| 5 | parseLlmJson — 非法 JSON | `parseLlmJson("not json")` → throw 包含"解析失败" |

---

## Task 12: Middleware — middleware.test.ts

**Files:**
- Create: `tests/unit/infra/middleware.test.ts`
- Test: `middleware.ts`

- [ ] **实现 5 个测试用例**

直接 import middleware 函数，构造 NextRequest 测试。需要设置 `process.env.JWT_SECRET`。

```typescript
import { middleware } from "@/middleware"
import { NextRequest } from "next/server"
import { SignJWT } from "jose"

async function makeToken() {
  const secret = new TextEncoder().encode(process.env.JWT_SECRET || "dev-secret-change-in-production")
  return new SignJWT({ userId: "u1", account: "test", role: "researcher" })
    .setProtectedHeader({ alg: "HS256" })
    .setExpirationTime("1h")
    .sign(secret)
}

function makeReq(path: string, token?: string) {
  const req = new NextRequest(new URL(path, "http://localhost:3000"))
  if (token) req.cookies.set("pentest_token", token)
  return req
}
```

| # | 测试 | 关键断言 |
|---|------|----------|
| 1 | 无 token 访问 API → 401 | `middleware(makeReq("/api/projects"))` → status 401 |
| 2 | 有 token 访问 API → next | response 无 status 401 |
| 3 | 公开 API 无需 token | `/api/auth/login`, `/api/health` → 不返回 401 |
| 4 | 无 token 访问页面 → redirect /login | response 是 redirect，location 包含 `/login` |
| 5 | 已登录访问 /login → redirect /dashboard | token 有效 + 访问 `/login` → redirect `/dashboard` |

---

## Task 13: API 路由 — auth.test.ts（详细模板）

**Files:**
- Create: `tests/integration/api/auth.test.ts`
- Test: `app/api/auth/login/route.ts`, `app/api/auth/logout/route.ts`

这是 API 路由测试的模板文件，后续 11 个文件参照此模式。

- [ ] **Step 1: 测试框架搭建**

```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach } from "vitest"
import { createTestDb, type TestDb } from "../../helpers/pglite-prisma"
import { NextRequest } from "next/server"
import bcrypt from "bcryptjs"

// vi.mock prisma 指向 PGlite 实例
let testDb: TestDb
vi.mock("@/lib/infra/prisma", () => ({ prisma: null as unknown }))

// vi.mock cookies()（login/logout 用到 setAuthCookie/clearAuthCookie）
const mockCookieStore = { set: vi.fn(), delete: vi.fn(), get: vi.fn() }
vi.mock("next/headers", () => ({ cookies: () => Promise.resolve(mockCookieStore) }))

beforeAll(async () => {
  testDb = await createTestDb()
  // 替换 mock 的 prisma 引用
  const mod = await import("@/lib/infra/prisma")
  ;(mod as any).prisma = testDb.prisma
})
afterAll(async () => { await testDb.cleanup() })
beforeEach(async () => {
  await testDb.truncateAll()
  mockCookieStore.set.mockClear()
  mockCookieStore.delete.mockClear()
})
```

- [ ] **Step 2: seed 测试用户**

```typescript
async function seedUser() {
  const hashed = await bcrypt.hash("correct-password", 10)
  return testDb.prisma.user.create({
    data: { account: "researcher@test.local", password: hashed, displayName: "Test", role: "researcher" },
  })
}
```

- [ ] **Step 3: 实现 6 个测试用例**

```typescript
import { POST as loginHandler } from "@/app/api/auth/login/route"
import { POST as logoutHandler } from "@/app/api/auth/logout/route"

describe("POST /api/auth/login", () => {
  it("正确凭据 → 200 + cookie", async () => {
    await seedUser()
    const req = new NextRequest("http://localhost/api/auth/login", {
      method: "POST",
      body: JSON.stringify({ account: "researcher@test.local", password: "correct-password" }),
    })
    const res = await loginHandler(req, { params: Promise.resolve({}) })
    expect(res.status).toBe(200)
    const body = await res.json()
    expect(body.user.account).toBe("researcher@test.local")
    expect(mockCookieStore.set).toHaveBeenCalled()
  })

  it("错误密码 → 401", async () => {
    await seedUser()
    const req = new NextRequest("http://localhost/api/auth/login", {
      method: "POST",
      body: JSON.stringify({ account: "researcher@test.local", password: "wrong" }),
    })
    const res = await loginHandler(req, { params: Promise.resolve({}) })
    expect(res.status).toBe(401)
  })

  it("不存在账号 → 401", async () => { /* 类似，account: "nobody" */ })
  it("空 body → 抛异常被 apiHandler 捕获 → 500", async () => { /* body: "{}" */ })
})

describe("POST /api/auth/logout", () => {
  it("正常 → 200 + 清除 cookie", async () => {
    const req = new NextRequest("http://localhost/api/auth/logout", { method: "POST" })
    const res = await logoutHandler(req, { params: Promise.resolve({}) })
    expect(res.status).toBe(200)
    expect(mockCookieStore.delete).toHaveBeenCalled()
  })
})
```

注意：auth 路由是 public API（middleware 不拦截），所以**不需要**测 "无 token → 401"。这里无需 `createAuthRequest`。

---

## Task 14: API 路由 — health.test.ts

**Files:** Create: `tests/integration/api/health.test.ts`

**测试点 (2 cases):**
- [ ] DB 正常 → `{status:"ok"}` (200)
- [ ] DB 连接失败 → `{status:"error"}` (503)

关键：mock `prisma.$queryRaw` 在第二个用例中 throw。

---

## Task 15: API 路由 — projects-crud.test.ts

**Files:** Create: `tests/integration/api/projects-crud.test.ts`

**覆盖路由:** GET/POST `/api/projects`, GET/PUT/DELETE `/api/projects/[projectId]`

**测试点 (10 cases):**
- [ ] list — 返回项目列表
- [ ] list — 空列表 → 200 + `[]`
- [ ] create — 有效参数 → 201 + project 对象
- [ ] create — 缺 name → 400
- [ ] get — 存在的项目 → 200
- [ ] get — 不存在 → 404
- [ ] update — 正常更新 name → 200
- [ ] update — 不存在 → 404
- [ ] delete — planning 状态下删除 → 200
- [ ] delete — running 状态下删除 → 400（lifecycle 拒绝）

关键：mock 对应的 service 层（`vi.mock("@/lib/services/project-service")`），用 factories 返回 mockProject。

---

## Task 16: API 路由 — projects-lifecycle.test.ts

**Files:** Create: `tests/integration/api/projects-lifecycle.test.ts`

**覆盖路由:** POST `/api/projects/[projectId]/start`, POST `/api/projects/[projectId]/stop`

**测试点 (10 cases):**
- [ ] start — 正常启动 → 202 + lifecycle 变更
- [ ] start — 重复启动 running 项目 → 409（InvalidTransitionError）
- [ ] start — 项目不存在 → 404
- [ ] stop — running 状态正常停止 → 202
- [ ] stop — 已 stopped 再 stop → 409
- [ ] stop — waiting_approval 下 stop
- [ ] start — planning 状态启动成功
- [ ] stop — 不存在项目 → 404
- [ ] start + stop 完整流程
- [ ] 非法 projectId 格式 → 404

关键：mock `projectService.startProject` / `stopProject`，根据场景 resolve 或 throw `DomainError`。

```typescript
vi.mock("@/lib/services/project-service", () => ({
  startProject: vi.fn(),
  stopProject: vi.fn(),
}))
// 在测试中：
import { startProject } from "@/lib/services/project-service"
;(startProject as Mock).mockRejectedValue(new InvalidTransitionError("running", "start"))
```

---

## Task 17: API 路由 — projects-approvals.test.ts

**Files:** Create: `tests/integration/api/projects-approvals.test.ts`

**测试点 (4 cases):**
- [ ] list approvals → 200 + 数组
- [ ] list — 空 → 200 + `[]`
- [ ] 项目不存在 → 404
- [ ] 过滤 status=pending

---

## Task 18: API 路由 — projects-assets.test.ts

**Files:** Create: `tests/integration/api/projects-assets.test.ts`

**测试点 (4 cases):**
- [ ] list assets → 200
- [ ] list — 空 → 200 + `[]`
- [ ] 项目不存在 → 404
- [ ] 带 kind 过滤参数

---

## Task 19: API 路由 — projects-findings.test.ts

**Files:** Create: `tests/integration/api/projects-findings.test.ts`

**测试点 (6 cases):**
- [ ] list findings → 200
- [ ] 空 → 200 + `[]`
- [ ] 项目不存在 → 404
- [ ] 按 severity 过滤
- [ ] 获取单个 finding 详情
- [ ] evidence 关联数据返回

---

## Task 20: API 路由 — projects-logs.test.ts

**Files:** Create: `tests/integration/api/projects-logs.test.ts`

**覆盖路由:** llm-logs, mcp-runs, pipeline-logs

**测试点 (6 cases):**
- [ ] llm-logs — 返回列表
- [ ] llm-logs — 空
- [ ] mcp-runs — 返回列表
- [ ] mcp-runs — 空
- [ ] pipeline-logs — 返回列表
- [ ] 项目不存在 → 404

---

## Task 21: API 路由 — projects-misc.test.ts

**Files:** Create: `tests/integration/api/projects-misc.test.ts`

**覆盖路由:** orchestrator, events, report-export, rounds/steps

**测试点 (8 cases):**
- [ ] orchestrator status → 200
- [ ] events SSE → 返回 ReadableStream
- [ ] report-export → 200 + JSON/PDF 格式
- [ ] report-export — 项目不存在 → 404
- [ ] rounds list → 200
- [ ] rounds — 空 → `[]`
- [ ] steps for round → 200
- [ ] steps — round 不存在 → 空数组

---

## Task 22: API 路由 — approvals-decision.test.ts

**Files:** Create: `tests/integration/api/approvals-decision.test.ts`

**覆盖路由:** PUT/PATCH `/api/approvals/[approvalId]`

**测试点 (6 cases):**
- [ ] approve pending → 202 + 状态变更
- [ ] reject pending → 202 + 状态变更
- [ ] 已决定再 decide → 400/409（ConflictError）
- [ ] 审批不存在 → 404
- [ ] 缺少 decision 字段 → 500
- [ ] PATCH 与 PUT 行为一致

关键：

```typescript
vi.mock("@/lib/services/approval-service", () => ({
  decide: vi.fn(),
}))
// approve:
;(decide as Mock).mockResolvedValue(mockApproval({ status: "approved", decidedAt: new Date() }))
// 已决定:
;(decide as Mock).mockRejectedValue(new ConflictError("Already decided"))
```

---

## Task 23: API 路由 — dashboard.test.ts

**Files:** Create: `tests/integration/api/dashboard.test.ts`

**测试点 (4 cases):**
- [ ] dashboard stats → 200 + 聚合数据
- [ ] dashboard — 无数据 → 200 + 零值
- [ ] llm-logs/recent → 200 + 最近日志列表
- [ ] llm-logs/recent — 空

---

## Task 24: API 路由 — settings.test.ts

**Files:** Create: `tests/integration/api/settings.test.ts`

**覆盖路由:** llm, approval-policy, system-status, mcp-tools, mcp-servers, mcp-sync

**测试点 (12 cases):**
- [ ] GET llm profiles → 200
- [ ] PUT llm profile — 更新成功
- [ ] GET approval-policy → 200
- [ ] PUT approval-policy → 更新成功
- [ ] GET system-status → 200
- [ ] GET mcp-tools → 200
- [ ] PUT mcp-tool enable/disable
- [ ] GET mcp-servers → 200
- [ ] POST mcp-server — 创建
- [ ] PUT mcp-server — 更新
- [ ] DELETE mcp-server → 200
- [ ] POST mcp-sync → 触发同步

关键：settings 路由依赖多个 service/repo，逐个 mock。mcp-sync 需 mock `registry.syncToolsFromServers`。

---

## 验收标准

1. 所有新测试通过: `npx vitest run --project unit --project integration`
2. MCP 层 (stdio-connector + registry) 覆盖率 >= 85%
3. LLM 层 (openai-provider + prompts + react-prompt + system-prompt) 覆盖率 >= 85%
4. middleware.ts 覆盖率 >= 90%
5. 新增约 120 个测试用例，零 skip

## 实施顺序建议

```
Task 1 (依赖安装) → Task 2-5 (助手) → Task 6-7 (MCP) → Task 8-11 (LLM)
→ Task 12 (middleware) → Task 13 (auth 模板) → Task 14-24 (其余路由)
```

Task 2-5 可并行；Task 6-7 可并行；Task 8-11 可并行；Task 14-24 可并行。
