# Phase 2 (P1): Worker 入口 + react-context + Services + Repositories + Infrastructure + Domain + 性能基准

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全 P1 模块测试：2 个 Worker/Context 测试 + 4 个 Service 测试 + 4 个 Repository 测试 + 5 个 Infrastructure 测试 + 2 个 Domain 测试 + 3 个性能基准，共 ~20 个文件 ~67 cases + 9 bench。

**Architecture:** Unit 测试用 `vi.mock` 隔离依赖；Integration 测试用 PGlite 内存数据库（复用 `tests/helpers/pglite-prisma.ts`）；性能基准用 `vitest bench` API。

**Tech Stack:** Vitest + vi.mock + PGlite + vitest bench

**依赖:** Phase 1 测试助手已就绪（mock-child-process、pglite-prisma 等）

---

## Task 1: Worker 入口 — worker.test.ts

**Files:**
- Create: `tests/unit/worker.test.ts`
- Test: `worker.ts`

- [ ] **Step 1: Mock 所有外部依赖**

```typescript
import { vi, describe, it, expect, beforeEach } from "vitest"

// mock pg-boss job queue
const mockQueue = {
  start: vi.fn().mockResolvedValue(undefined),
  stop: vi.fn().mockResolvedValue(undefined),
  publish: vi.fn().mockResolvedValue("job-1"),
  subscribe: vi.fn().mockResolvedValue(undefined),
  cancelByProject: vi.fn().mockResolvedValue(0),
}
vi.mock("@/lib/infra/job-queue", () => ({
  createPgBossJobQueue: () => mockQueue,
  getBoss: () => ({ on: vi.fn() }),
}))

// mock project-repo
const mockProjectRepo = {
  findByLifecycles: vi.fn().mockResolvedValue([]),
  updateLifecycle: vi.fn().mockResolvedValue(undefined),
}
vi.mock("@/lib/repositories/project-repo", () => mockProjectRepo)

// mock mcp-run-repo
const mockMcpRunRepo = {
  failStaleRunningRuns: vi.fn().mockResolvedValue(0),
  countPendingByProject: vi.fn().mockResolvedValue(0),
}
vi.mock("@/lib/repositories/mcp-run-repo", () => mockMcpRunRepo)

// mock registry closeAll
vi.mock("@/lib/mcp/registry", () => ({
  closeAll: vi.fn().mockResolvedValue(undefined),
}))

// mock llm-log-repo、mcp-bootstrap、logger、pipeline-logger
vi.mock("@/lib/repositories/llm-log-repo", () => ({
  cleanupStale: vi.fn().mockResolvedValue({ count: 0 }),
}))
vi.mock("@/lib/services/mcp-bootstrap", () => ({
  bootstrapMcp: vi.fn().mockResolvedValue({ servers: { loaded: 2, errors: [] }, tools: { synced: 5, errors: [] } }),
}))
vi.mock("@/lib/infra/logger", () => ({
  logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn(), fatal: vi.fn() },
}))
vi.mock("@/lib/infra/pipeline-logger", () => ({
  createPipelineLogger: () => ({ warn: vi.fn(), info: vi.fn(), error: vi.fn() }),
}))
```

- [ ] **Step 2: 测试 recoverStaleProjects — 有 stale 项目**

提取 `recoverStaleProjects` 逻辑进行测试。由于 worker.ts 是顶层脚本，需要 mock `process.on` 和 `process.exit` 后用动态 import 测试内部逻辑。实际策略：将 worker.ts 中的 `recoverStaleProjects` 提取为可测试函数，或直接 mock 测试 publish 调用。

```typescript
describe("recoverStaleProjects", () => {
  it("planning 状态 → 重新入队 react_round", async () => {
    mockProjectRepo.findByLifecycles.mockResolvedValue([
      { id: "p1", lifecycle: "planning", currentRound: 2 },
    ])
    // 调用 recoverStaleProjects（通过 import worker 模块触发，或提取函数）
    // 验证 queue.publish 被调用
    expect(mockQueue.publish).toHaveBeenCalledWith(
      "react_round",
      { projectId: "p1", round: 3 },
      expect.objectContaining({ expireInSeconds: 1800 }),
    )
  })

  it("stopping 状态 → 强制转为 stopped", async () => {
    mockProjectRepo.findByLifecycles.mockResolvedValue([
      { id: "p2", lifecycle: "stopping", currentRound: 1 },
    ])
    expect(mockProjectRepo.updateLifecycle).toHaveBeenCalledWith("p2", "stopped")
  })
})
```

- [ ] **Step 3: 测试 recoverStaleProjects — 无 stale 项目**

```typescript
it("无 stale 项目 → 空跑不报错", async () => {
  mockProjectRepo.findByLifecycles.mockResolvedValue([])
  // 验证 publish 未被调用
  expect(mockQueue.publish).not.toHaveBeenCalled()
})
```

- [ ] **Step 4: 测试 fatalShutdown**

```typescript
describe("fatalShutdown", () => {
  it("调用 closeAll + process.exit(1)", async () => {
    const exitSpy = vi.spyOn(process, "exit").mockImplementation(() => undefined as never)
    const { closeAll } = await import("@/lib/mcp/registry")
    // 触发 fatalShutdown
    process.emit("uncaughtException", new Error("boom"))
    await vi.waitFor(() => expect(exitSpy).toHaveBeenCalledWith(1))
    expect(closeAll).toHaveBeenCalled()
    exitSpy.mockRestore()
  })

  it("closeAll 抛异常 → best-effort 不二次崩溃", async () => {
    const { closeAll } = await import("@/lib/mcp/registry")
    ;(closeAll as ReturnType<typeof vi.fn>).mockRejectedValue(new Error("cleanup failed"))
    const exitSpy = vi.spyOn(process, "exit").mockImplementation(() => undefined as never)
    process.emit("uncaughtException", new Error("boom"))
    await vi.waitFor(() => expect(exitSpy).toHaveBeenCalledWith(1))
    exitSpy.mockRestore()
  })
})
```

- [ ] **Step 5: 测试 handler 注册**

```typescript
it("注册 5 个 job handler", async () => {
  // main() 启动后 subscribe 被调用 5 次
  expect(mockQueue.subscribe).toHaveBeenCalledTimes(5)
  const names = mockQueue.subscribe.mock.calls.map((c: unknown[]) => c[0])
  expect(names).toEqual(expect.arrayContaining([
    "react_round", "analyze_result", "verify_finding", "round_completed", "settle_closure",
  ]))
})
```

---

## Task 2: react-context — react-context.test.ts

**Files:**
- Create: `tests/unit/workers/react-context.test.ts`
- Test: `lib/workers/react-context.ts`

- [ ] **Step 1: 基础导入和工厂函数**

```typescript
import { describe, it, expect } from "vitest"
import { ReactContextManager, generateToolCallId } from "@/lib/workers/react-context"

function addSteps(ctx: ReactContextManager, count: number) {
  for (let i = 1; i <= count; i++) {
    const callId = generateToolCallId()
    ctx.addAssistantMessage(`思考第${i}步`, { name: "nmap_scan", arguments: `{"t":"${i}"}` }, callId)
    ctx.addToolResult({
      stepIndex: i,
      toolName: "nmap_scan",
      target: `192.168.1.${i}`,
      functionName: "nmap_scan",
      output: `端口扫描结果 step ${i}: ${"x".repeat(100)}`,
      status: "succeeded",
      toolCallId: callId,
    })
  }
}
```

- [ ] **Step 2: 短历史不压缩**

```typescript
it("步数 <= RECENT_WINDOW 时不压缩，原样返回", () => {
  const ctx = new ReactContextManager("system prompt", "开始渗透测试")
  addSteps(ctx, 3)
  const msgs = ctx.getMessages()
  // system + user + 3*(assistant+tool) = 2 + 6 = 8
  expect(msgs).toHaveLength(8)
  expect(msgs[0].role).toBe("system")
  expect(msgs[1].role).toBe("user")
})
```

- [ ] **Step 3: 超长历史压缩**

```typescript
it("步数超过 RECENT_WINDOW 且 token 超限时，旧消息被摘要替代", () => {
  const ctx = new ReactContextManager("system prompt", "开始渗透测试")
  // 添加大量步骤触发压缩（每步大 output 以超过 TOKEN_BUDGET）
  for (let i = 1; i <= 10; i++) {
    const callId = generateToolCallId()
    ctx.addAssistantMessage(`思考${i}`, { name: "scan", arguments: "{}" }, callId)
    ctx.addToolResult({
      stepIndex: i, toolName: "scan", target: "t", functionName: "scan",
      output: "x".repeat(30000), // 大输出加速触发压缩
      status: "succeeded", toolCallId: callId,
    })
  }
  const msgs = ctx.getMessages()
  // 压缩后应有 summary 消息
  const summaryMsg = msgs.find(m => m.content?.includes("[Previous steps summary]"))
  expect(summaryMsg).toBeDefined()
})
```

- [ ] **Step 4: system 消息保留**

```typescript
it("压缩后 system 消息始终保留在首位", () => {
  const ctx = new ReactContextManager("你是安全AI", "开始")
  for (let i = 1; i <= 10; i++) {
    const callId = generateToolCallId()
    ctx.addAssistantMessage(`t${i}`, { name: "s", arguments: "{}" }, callId)
    ctx.addToolResult({
      stepIndex: i, toolName: "s", target: "t", functionName: "s",
      output: "x".repeat(30000), status: "succeeded", toolCallId: callId,
    })
  }
  const msgs = ctx.getMessages()
  expect(msgs[0]).toEqual({ role: "system", content: "你是安全AI" })
})
```

- [ ] **Step 5: 空历史不崩溃**

```typescript
it("空历史 → getMessages 返回 system + user", () => {
  const ctx = new ReactContextManager("sys", "usr")
  const msgs = ctx.getMessages()
  expect(msgs).toHaveLength(2)
  expect(msgs[0].role).toBe("system")
  expect(msgs[1].role).toBe("user")
})
```

---

## Task 3: Services — settings-service.test.ts (integration)

**Files:**
- Create: `tests/integration/services/settings-service.test.ts`
- Test: `lib/services/settings-service.ts`

- [ ] **Step 1: PGlite 搭建**

```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach, vi } from "vitest"
import { createTestDb, type TestDb } from "../../helpers/pglite-prisma"

let testDb: TestDb
vi.mock("@/lib/infra/prisma", () => ({ prisma: null as unknown }))

beforeAll(async () => {
  testDb = await createTestDb()
  const mod = await import("@/lib/infra/prisma")
  ;(mod as any).prisma = testDb.prisma
})
afterAll(async () => { await testDb.cleanup() })
beforeEach(async () => { await testDb.truncateAll() })
```

- [ ] **Step 2: 实现 4 个测试用例**

```typescript
import { getLlmProfiles, upsertLlmProfile, getGlobalConfig, getSystemStatus } from "@/lib/services/settings-service"

it("getLlmProfiles — 空 → 空数组", async () => {
  expect(await getLlmProfiles()).toEqual([])
})

it("upsertLlmProfile — 创建 + 读取", async () => {
  await upsertLlmProfile("planner", { model: "gpt-4", provider: "openai" })
  const profiles = await getLlmProfiles()
  expect(profiles).toHaveLength(1)
  expect(profiles[0].model).toBe("gpt-4")
})

it("getGlobalConfig — 首次调用自动创建默认值", async () => {
  const config = await getGlobalConfig()
  expect(config).toBeDefined()
  expect(config.id).toBe("global")
})

it("getSystemStatus — 返回统计信息", async () => {
  const status = await getSystemStatus()
  expect(status.database).toBe("connected")
  expect(typeof status.tools).toBe("number")
})
```

---

## Task 4: Services — dashboard-service.test.ts (integration)

**Files:**
- Create: `tests/integration/services/dashboard-service.test.ts`
- Test: `lib/services/dashboard-service.ts`

- [ ] **Step 1: PGlite + mock project-repo**

```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach, vi } from "vitest"
import { createTestDb, type TestDb } from "../../helpers/pglite-prisma"

let testDb: TestDb
vi.mock("@/lib/infra/prisma", () => ({ prisma: null as unknown }))

beforeAll(async () => {
  testDb = await createTestDb()
  const mod = await import("@/lib/infra/prisma")
  ;(mod as any).prisma = testDb.prisma
})
afterAll(async () => { await testDb.cleanup() })
beforeEach(async () => { await testDb.truncateAll() })
```

- [ ] **Step 2: 实现 3 个测试用例**

```typescript
import { getDashboardData } from "@/lib/services/dashboard-service"

it("无数据 → 返回零值聚合", async () => {
  const data = await getDashboardData()
  expect(data.projectCount).toBe(0)
  expect(data.activeCount).toBe(0)
  expect(data.findingStats).toEqual([])
})

it("有项目数据 → projectCount 正确", async () => {
  await testDb.prisma.project.create({
    data: { name: "test", targetUrls: ["http://example.com"], lifecycle: "idle" },
  })
  const data = await getDashboardData()
  expect(data.projectCount).toBe(1)
})

it("recentProjects 最多 5 条", async () => {
  for (let i = 0; i < 7; i++) {
    await testDb.prisma.project.create({
      data: { name: `p${i}`, targetUrls: ["http://example.com"], lifecycle: "idle" },
    })
  }
  const data = await getDashboardData()
  expect(data.recentProjects.length).toBeLessThanOrEqual(5)
})
```

---

## Task 5: Services — mcp-bootstrap.test.ts (integration)

**Files:**
- Create: `tests/integration/services/mcp-bootstrap.test.ts`
- Test: `lib/services/mcp-bootstrap.ts`

- [ ] **Step 1: Mock fs + registry**

```typescript
import { describe, it, expect, vi } from "vitest"
import { readFileSync } from "fs"

vi.mock("fs", () => ({ readFileSync: vi.fn() }))
vi.mock("@/lib/mcp/registry", () => ({
  syncToolsFromServers: vi.fn().mockResolvedValue({ synced: 3, errors: [] }),
}))

// mock mcp-tool-repo upsertServer
vi.mock("@/lib/repositories/mcp-tool-repo", () => ({
  upsertServer: vi.fn().mockResolvedValue(undefined),
}))
```

- [ ] **Step 2: 实现 3 个测试用例**

```typescript
import { loadServersFromManifest, bootstrapMcp } from "@/lib/services/mcp-bootstrap"

it("manifest 正常 → 加载 server 数量正确", async () => {
  ;(readFileSync as ReturnType<typeof vi.fn>).mockReturnValue(JSON.stringify({
    mcpServers: { "script": { command: "npx", args: ["tsx", "main.ts"] } }
  }))
  const result = await loadServersFromManifest()
  expect(result.loaded).toBe(1)
  expect(result.errors).toHaveLength(0)
})

it("manifest 文件不存在 → 返回错误", async () => {
  ;(readFileSync as ReturnType<typeof vi.fn>).mockImplementation(() => { throw new Error("ENOENT") })
  const result = await loadServersFromManifest()
  expect(result.loaded).toBe(0)
  expect(result.errors[0]).toContain("ENOENT")
})

it("bootstrapMcp — 完整流程", async () => {
  ;(readFileSync as ReturnType<typeof vi.fn>).mockReturnValue(JSON.stringify({
    mcpServers: { "s1": { command: "node", args: ["index.js"] } }
  }))
  const result = await bootstrapMcp()
  expect(result.servers.loaded).toBe(1)
  expect(result.tools.synced).toBe(3)
})
```

---

## Task 6: Services — asset-service.test.ts (unit)

**Files:**
- Create: `tests/unit/services/asset-service.test.ts`
- Test: `lib/services/asset-service.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect, vi } from "vitest"

const mockAssetRepo = {
  findByProject: vi.fn(),
  findTreeRoots: vi.fn(),
  findById: vi.fn(),
}
vi.mock("@/lib/repositories/asset-repo", () => mockAssetRepo)

import { listByProject, getAssetTree, getAsset } from "@/lib/services/asset-service"
import { NotFoundError } from "@/lib/domain/errors"

it("listByProject — 委托 repo", async () => {
  mockAssetRepo.findByProject.mockResolvedValue([{ id: "a1" }])
  const result = await listByProject("p1")
  expect(result).toEqual([{ id: "a1" }])
  expect(mockAssetRepo.findByProject).toHaveBeenCalledWith("p1")
})

it("getAsset — 存在 → 返回", async () => {
  mockAssetRepo.findById.mockResolvedValue({ id: "a1", kind: "host" })
  const result = await getAsset("a1")
  expect(result.id).toBe("a1")
})

it("getAsset — 不存在 → throw NotFoundError", async () => {
  mockAssetRepo.findById.mockResolvedValue(null)
  await expect(getAsset("nope")).rejects.toThrow(NotFoundError)
})
```

---

## Task 7: Repositories — evidence-repo.test.ts (integration)

**Files:**
- Create: `tests/integration/repositories/evidence-repo.test.ts`
- Test: `lib/repositories/evidence-repo.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach, vi } from "vitest"
import { createTestDb, type TestDb } from "../../helpers/pglite-prisma"

let testDb: TestDb
vi.mock("@/lib/infra/prisma", () => ({ prisma: null as unknown }))

beforeAll(async () => {
  testDb = await createTestDb()
  const mod = await import("@/lib/infra/prisma")
  ;(mod as any).prisma = testDb.prisma
})
afterAll(async () => { await testDb.cleanup() })
beforeEach(async () => { await testDb.truncateAll() })

import { findByProject, create } from "@/lib/repositories/evidence-repo"

it("create + findByProject", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "test", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  await create({ projectId: project.id, title: "XSS", toolName: "zap", rawOutput: "<script>" })
  const list = await findByProject(project.id)
  expect(list).toHaveLength(1)
  expect(list[0].title).toBe("XSS")
})

it("findByProject — 空", async () => {
  const list = await findByProject("nonexistent")
  expect(list).toEqual([])
})

it("create — 带可选字段", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t2", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  const ev = await create({
    projectId: project.id, title: "SQLi", toolName: "sqlmap",
    rawOutput: "injected", summary: "SQL注入", artifactPaths: ["/tmp/dump.txt"],
  })
  expect(ev.summary).toBe("SQL注入")
  expect(ev.artifactPaths).toContain("/tmp/dump.txt")
})
```

---

## Task 8: Repositories — llm-log-repo.test.ts (integration)

**Files:**
- Create: `tests/integration/repositories/llm-log-repo.test.ts`
- Test: `lib/repositories/llm-log-repo.ts`

- [ ] **实现 3 个测试用例**

```typescript
// PGlite 搭建同 Task 7（省略重复）
import { create, complete, fail, cleanupStale } from "@/lib/repositories/llm-log-repo"

it("create → status=streaming, complete → status=completed", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  const log = await create({ projectId: project.id, role: "planner", phase: "recon", prompt: "test", model: "gpt-4", provider: "openai" })
  expect(log.status).toBe("streaming")
  const updated = await complete(log.id, "response text", 1500)
  expect(updated.status).toBe("completed")
  expect(updated.durationMs).toBe(1500)
})

it("fail → status=failed + error 信息", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t2", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  const log = await create({ projectId: project.id, role: "planner", phase: "recon", prompt: "p", model: "gpt-4", provider: "openai" })
  const updated = await fail(log.id, "timeout")
  expect(updated.status).toBe("failed")
  expect(updated.error).toBe("timeout")
})

it("cleanupStale — 清理超时 streaming 记录", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t3", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  // 手动创建一条旧 streaming 记录
  await testDb.prisma.llmCallLog.create({
    data: {
      projectId: project.id, role: "planner", phase: "recon",
      prompt: "old", model: "gpt-4", provider: "openai", status: "streaming",
      createdAt: new Date(Date.now() - 20 * 60 * 1000), // 20 分钟前
    },
  })
  const result = await cleanupStale(10)
  expect(result.count).toBeGreaterThanOrEqual(1)
})
```

---

## Task 9: Repositories — mcp-tool-repo.test.ts (integration)

**Files:**
- Create: `tests/integration/repositories/mcp-tool-repo.test.ts`
- Test: `lib/repositories/mcp-tool-repo.ts`

- [ ] **实现 4 个测试用例**

```typescript
// PGlite 搭建同上
import { findAll, findEnabled, upsert, findByToolName } from "@/lib/repositories/mcp-tool-repo"

it("upsert + findAll", async () => {
  await upsert({ serverName: "script", toolName: "nmap_scan", capability: "recon" })
  const all = await findAll()
  expect(all).toHaveLength(1)
  expect(all[0].toolName).toBe("nmap_scan")
})

it("findEnabled — 只返回 enabled=true", async () => {
  await upsert({ serverName: "s1", toolName: "t1", capability: "recon" })
  await testDb.prisma.mcpTool.update({ where: { toolName: "t1" }, data: { enabled: false } })
  const enabled = await findEnabled()
  expect(enabled).toHaveLength(0)
})

it("findByToolName — 存在 vs 不存在", async () => {
  await upsert({ serverName: "s1", toolName: "zap_scan", capability: "vuln" })
  expect(await findByToolName("zap_scan")).toBeDefined()
  expect(await findByToolName("nope")).toBeNull()
})

it("upsert — 重复调用更新而非重复创建", async () => {
  await upsert({ serverName: "s1", toolName: "t1", capability: "recon" })
  await upsert({ serverName: "s2", toolName: "t1", capability: "vuln" })
  const tool = await findByToolName("t1")
  expect(tool!.serverName).toBe("s2")
  expect(tool!.capability).toBe("vuln")
  expect(await findAll()).toHaveLength(1)
})
```

---

## Task 10: Repositories — pipeline-log-repo.test.ts (integration)

**Files:**
- Create: `tests/integration/repositories/pipeline-log-repo.test.ts`
- Test: `lib/repositories/pipeline-log-repo.ts`

- [ ] **实现 3 个测试用例**

```typescript
// PGlite 搭建同上
import { create, findByProject, cleanupOld } from "@/lib/repositories/pipeline-log-repo"

it("create + findByProject", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  await create({ projectId: project.id, jobType: "react_round", stage: "start", level: "info", message: "轮次开始" })
  const logs = await findByProject(project.id)
  expect(logs).toHaveLength(1)
  expect(logs[0].message).toBe("轮次开始")
})

it("findByProject — level 过滤（info 过滤掉 debug）", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t2", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  await create({ projectId: project.id, jobType: "react", stage: "s", level: "debug", message: "debug msg" })
  await create({ projectId: project.id, jobType: "react", stage: "s", level: "info", message: "info msg" })
  const logs = await findByProject(project.id, { level: "info" })
  expect(logs).toHaveLength(1)
  expect(logs[0].level).toBe("info")
})

it("cleanupOld — 删除旧 debug 日志", async () => {
  const project = await testDb.prisma.project.create({
    data: { name: "t3", targetUrls: ["http://t.com"], lifecycle: "idle" },
  })
  await testDb.prisma.pipelineLog.create({
    data: {
      projectId: project.id, jobType: "react", stage: "s", level: "debug",
      message: "old", createdAt: new Date(Date.now() - 40 * 24 * 60 * 60 * 1000),
    },
  })
  const deleted = await cleanupOld(30)
  expect(deleted).toBeGreaterThanOrEqual(1)
})
```

---

## Task 11: Infrastructure — event-bus.test.ts (unit)

**Files:**
- Create: `tests/unit/infra/event-bus.test.ts`
- Test: `lib/infra/event-bus.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect, vi } from "vitest"

// mock prisma.$executeRawUnsafe
const mockPrisma = { $executeRawUnsafe: vi.fn().mockResolvedValue(undefined) }
vi.mock("@/lib/infra/prisma", () => ({ prisma: mockPrisma }))

import { publishEvent } from "@/lib/infra/event-bus"

it("发布事件 → 调用 pg_notify", async () => {
  await publishEvent({ type: "status_change", projectId: "p1", timestamp: new Date().toISOString(), data: {} })
  expect(mockPrisma.$executeRawUnsafe).toHaveBeenCalledWith(
    expect.stringContaining("pg_notify"),
    expect.stringContaining("status_change"),
  )
})

it("超长 payload → 截断", async () => {
  const bigData: Record<string, unknown> = { blob: "x".repeat(8000) }
  await publishEvent({ type: "big", projectId: "p1", timestamp: new Date().toISOString(), data: bigData })
  // 验证实际发送的 payload 包含 truncated 标记
  const sentPayload = mockPrisma.$executeRawUnsafe.mock.calls[0][1] as string
  expect(sentPayload).toContain("truncated")
})

it("正常大小 payload → 不截断", async () => {
  mockPrisma.$executeRawUnsafe.mockClear()
  await publishEvent({ type: "small", projectId: "p1", timestamp: new Date().toISOString(), data: { ok: true } })
  const sentPayload = mockPrisma.$executeRawUnsafe.mock.calls[0][1] as string
  expect(sentPayload).not.toContain("truncated")
})
```

---

## Task 12: Infrastructure — abort-registry.test.ts (unit)

**Files:**
- Create: `tests/unit/infra/abort-registry.test.ts`
- Test: `lib/infra/abort-registry.ts`

- [ ] **实现 4 个测试用例**

```typescript
import { describe, it, expect, beforeEach } from "vitest"
import { registerAbort, unregisterAbort, abortAllForProject } from "@/lib/infra/abort-registry"

// 注意：模块级 Map 状态共享，需要每次用 abortAllForProject 清理
beforeEach(() => { abortAllForProject("test-project") })

it("registerAbort + abortAllForProject → controller 被 abort", () => {
  const ac = new AbortController()
  registerAbort("p1", ac)
  abortAllForProject("p1")
  expect(ac.signal.aborted).toBe(true)
})

it("unregisterAbort → 不再被 abort", () => {
  const ac = new AbortController()
  registerAbort("p1", ac)
  unregisterAbort("p1", ac)
  abortAllForProject("p1")
  expect(ac.signal.aborted).toBe(false)
})

it("多个 controller → 全部被 abort", () => {
  const ac1 = new AbortController()
  const ac2 = new AbortController()
  registerAbort("p1", ac1)
  registerAbort("p1", ac2)
  abortAllForProject("p1")
  expect(ac1.signal.aborted).toBe(true)
  expect(ac2.signal.aborted).toBe(true)
})

it("abortAllForProject — 不存在的项目 → 不崩溃", () => {
  expect(() => abortAllForProject("nonexistent")).not.toThrow()
})
```

---

## Task 13: Infrastructure — auth.test.ts (unit)

**Files:**
- Create: `tests/unit/infra/auth.test.ts`
- Test: `lib/infra/auth.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect, vi } from "vitest"

// mock next/headers cookies
const mockCookieStore = { set: vi.fn(), delete: vi.fn(), get: vi.fn() }
vi.mock("next/headers", () => ({ cookies: () => Promise.resolve(mockCookieStore) }))

import { signToken, verifyToken, requireAuth } from "@/lib/infra/auth"

it("signToken + verifyToken — 往返一致", async () => {
  const payload = { userId: "u1", account: "test@local", role: "researcher" }
  const token = await signToken(payload)
  const decoded = await verifyToken(token)
  expect(decoded.userId).toBe("u1")
  expect(decoded.account).toBe("test@local")
})

it("verifyToken — 非法 token → throw", async () => {
  await expect(verifyToken("invalid-jwt")).rejects.toThrow()
})

it("requireAuth — 无 cookie → throw UnauthorizedError", async () => {
  mockCookieStore.get.mockReturnValue(undefined)
  const { UnauthorizedError } = await import("@/lib/domain/errors")
  await expect(requireAuth()).rejects.toThrow(UnauthorizedError)
})
```

---

## Task 14: Infrastructure — api-handler.test.ts (unit)

**Files:**
- Create: `tests/unit/infra/api-handler.test.ts`
- Test: `lib/infra/api-handler.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect } from "vitest"
import { apiHandler, json } from "@/lib/infra/api-handler"
import { DomainError, NotFoundError } from "@/lib/domain/errors"

it("正常 handler → 返回原始 Response", async () => {
  const handler = apiHandler(async () => json({ ok: true }))
  const res = await handler(new Request("http://localhost/api/test"), { params: Promise.resolve({}) })
  expect(res.status).toBe(200)
  expect(await res.json()).toEqual({ ok: true })
})

it("handler 抛 DomainError → 返回对应 status + error body", async () => {
  const handler = apiHandler(async () => { throw new NotFoundError("Project", "p1") })
  const res = await handler(new Request("http://localhost/api/test"), { params: Promise.resolve({}) })
  expect(res.status).toBe(404)
  const body = await res.json()
  expect(body.code).toBe("NOT_FOUND")
})

it("handler 抛未知异常 → 500 Internal", async () => {
  const handler = apiHandler(async () => { throw new Error("boom") })
  const res = await handler(new Request("http://localhost/api/test"), { params: Promise.resolve({}) })
  expect(res.status).toBe(500)
  const body = await res.json()
  expect(body.code).toBe("INTERNAL")
})
```

---

## Task 15: Infrastructure — pg-listener.test.ts (unit)

**Files:**
- Create: `tests/unit/infra/pg-listener.test.ts`
- Test: `lib/infra/pg-listener.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest"
import { EventEmitter } from "events"

// mock pg.Client
class MockPgClient extends EventEmitter {
  connect = vi.fn().mockResolvedValue(undefined)
  query = vi.fn().mockResolvedValue(undefined)
  end = vi.fn().mockResolvedValue(undefined)
}
let mockClient: MockPgClient
vi.mock("pg", () => ({
  default: { Client: class { constructor() { return mockClient } } },
}))

beforeEach(() => { mockClient = new MockPgClient() })

import { createPgListener } from "@/lib/infra/pg-listener"

it("创建 listener → 执行 LISTEN 命令", async () => {
  const cb = vi.fn()
  await createPgListener("project_events", cb)
  expect(mockClient.query).toHaveBeenCalledWith('LISTEN "project_events"')
})

it("收到 notification → 调用 callback", async () => {
  const cb = vi.fn()
  await createPgListener("project_events", cb)
  mockClient.emit("notification", { channel: "project_events", payload: '{"type":"test"}' })
  expect(cb).toHaveBeenCalledWith('{"type":"test"}')
})

it("非法 channel 名 → throw", async () => {
  await expect(createPgListener("bad;channel", vi.fn())).rejects.toThrow("Invalid channel")
})
```

---

## Task 16: Domain — risk-policy.test.ts (unit)

**Files:**
- Create: `tests/unit/domain/risk-policy.test.ts`
- Test: `lib/domain/risk-policy.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect } from "vitest"
import { requiresApproval, type ApprovalPolicy } from "@/lib/domain/risk-policy"

const enabledPolicy: ApprovalPolicy = { approvalEnabled: true, autoApproveLowRisk: true, autoApproveMediumRisk: false }
const disabledPolicy: ApprovalPolicy = { approvalEnabled: false, autoApproveLowRisk: false, autoApproveMediumRisk: false }

it("approvalEnabled=false → 一律不需要审批", () => {
  expect(requiresApproval("high", disabledPolicy)).toBe(false)
  expect(requiresApproval("low", disabledPolicy)).toBe(false)
})

it("low risk + autoApproveLow=true → 不需要审批", () => {
  expect(requiresApproval("low", enabledPolicy)).toBe(false)
})

it("medium risk + autoApproveMedium=false → 需要审批", () => {
  expect(requiresApproval("medium", enabledPolicy)).toBe(true)
})
```

---

## Task 17: Domain — scope-policy.test.ts (unit)

**Files:**
- Create: `tests/unit/domain/scope-policy.test.ts`
- Test: `lib/domain/scope-policy.ts`

- [ ] **实现 3 个测试用例**

```typescript
import { describe, it, expect } from "vitest"
import { createScopePolicy, extractHost, isIPv4 } from "@/lib/domain/scope-policy"

it("域名目标 → 同根域子域名在 scope 内", () => {
  const policy = createScopePolicy(["https://app.example.com"])
  expect(policy.isInScope("api.example.com")).toBe(true)
  expect(policy.isInScope("https://other.example.com:8080/path")).toBe(true)
  expect(policy.isInScope("evil.com")).toBe(false)
})

it("IP 目标 → 同 /24 子网在 scope 内", () => {
  const policy = createScopePolicy(["192.168.1.100"])
  expect(policy.isInScope("192.168.1.200")).toBe(true)
  expect(policy.isInScope("192.168.2.1")).toBe(false)
})

it("extractHost — 各种格式提取正确", () => {
  expect(extractHost("https://example.com:8080/path")).toBe("example.com")
  expect(extractHost("tcp://10.0.0.1:22")).toBe("10.0.0.1")
  expect(extractHost("user@host.local")).toBe("host.local")
})
```

---

## Task 18: 性能基准 — mcp-connector.bench.ts

**Files:**
- Create: `tests/perf/mcp-connector.bench.ts`

- [ ] **Step 1: vitest.config.mts 添加 bench 配置**

在 `vitest.config.mts` 的 `test` 对象中添加：
```typescript
benchmark: {
  include: ["tests/perf/**/*.bench.ts"],
},
```

- [ ] **Step 2: 实现 3 个基准测试**

```typescript
import { bench, describe } from "vitest"
import { EventEmitter, PassThrough } from "stream"

// 模拟 JSON-RPC 请求/响应往返
function simulateRpcRoundtrip() {
  const stdout = new PassThrough()
  const request = JSON.stringify({ jsonrpc: "2.0", id: 1, method: "tools/call", params: {} })
  const response = JSON.stringify({ jsonrpc: "2.0", id: 1, result: { content: [{ type: "text", text: "ok" }] } })

  return new Promise<void>((resolve) => {
    stdout.on("data", () => resolve())
    stdout.write(request)
    stdout.write(response + "\n")
  })
}

describe("MCP Connector 性能", () => {
  bench("单请求 JSON-RPC 往返", async () => {
    await simulateRpcRoundtrip()
  }, { time: 1000 })

  bench("并发 10 请求吞吐", async () => {
    await Promise.all(Array.from({ length: 10 }, () => simulateRpcRoundtrip()))
  }, { time: 1000 })

  bench("JSON.parse + JSON.stringify 序列化开销", () => {
    const obj = { jsonrpc: "2.0", id: 1, result: { content: [{ type: "text", text: "scan result ".repeat(50) }] } }
    const str = JSON.stringify(obj)
    JSON.parse(str)
  }, { time: 1000 })
})
```

---

## Task 19: 性能基准 — react-loop.bench.ts

**Files:**
- Create: `tests/perf/react-loop.bench.ts`

- [ ] **实现 3 个基准测试**

```typescript
import { bench, describe } from "vitest"
import { ReactContextManager, generateToolCallId } from "@/lib/workers/react-context"

function buildContext(steps: number): ReactContextManager {
  const ctx = new ReactContextManager("system prompt", "开始测试")
  for (let i = 1; i <= steps; i++) {
    const callId = generateToolCallId()
    ctx.addAssistantMessage(`思考${i}`, { name: "scan", arguments: `{"i":${i}}` }, callId)
    ctx.addToolResult({
      stepIndex: i, toolName: "scan", target: "target",
      functionName: "scan", output: `结果${i}: ${"data".repeat(200)}`,
      status: "succeeded", toolCallId: callId,
    })
  }
  return ctx
}

describe("ReAct Loop 性能", () => {
  bench("单轮 context 构建 + getMessages", () => {
    const ctx = new ReactContextManager("sys", "usr")
    const callId = generateToolCallId()
    ctx.addAssistantMessage("think", { name: "scan", arguments: "{}" }, callId)
    ctx.addToolResult({
      stepIndex: 1, toolName: "scan", target: "t",
      functionName: "scan", output: "result", status: "succeeded", toolCallId: callId,
    })
    ctx.getMessages()
  }, { time: 1000 })

  bench("5 轮 context 构建", () => {
    buildContext(5)
  }, { time: 1000 })

  bench("10 轮 context（触发压缩）构建", () => {
    const ctx = new ReactContextManager("sys", "usr")
    for (let i = 1; i <= 10; i++) {
      const callId = generateToolCallId()
      ctx.addAssistantMessage(`t${i}`, { name: "s", arguments: "{}" }, callId)
      ctx.addToolResult({
        stepIndex: i, toolName: "s", target: "t", functionName: "s",
        output: "x".repeat(30000), status: "succeeded", toolCallId: callId,
      })
    }
  }, { time: 2000 })
})
```

---

## Task 20: 性能基准 — job-queue.bench.ts

**Files:**
- Create: `tests/perf/job-queue.bench.ts`

- [ ] **实现 3 个基准测试（纯内存模拟）**

由于 pg-boss 需要真实数据库，这里测量 job 数据序列化和消息构造的开销。

```typescript
import { bench, describe } from "vitest"

// 模拟 job publish 的数据序列化开销
function simulatePublish(jobName: string, data: unknown) {
  const serialized = JSON.stringify({ name: jobName, data, options: { retryLimit: 3, expireInSeconds: 600 } })
  JSON.parse(serialized)
  return serialized
}

describe("Job Queue 性能", () => {
  bench("单次 publish 数据序列化", () => {
    simulatePublish("react_round", { projectId: "p1", round: 1 })
  }, { time: 1000 })

  bench("批量 100 次 publish 序列化", () => {
    for (let i = 0; i < 100; i++) {
      simulatePublish("react_round", { projectId: `p${i}`, round: i })
    }
  }, { time: 1000 })

  bench("publish + fetch 数据往返（序列化+反序列化）", () => {
    const data = { projectId: "p1", round: 5, metadata: { tools: ["nmap", "zap"], target: "192.168.1.0/24" } }
    const wire = JSON.stringify(data)
    const restored = JSON.parse(wire)
    if (restored.projectId !== "p1") throw new Error("corruption")
  }, { time: 1000 })
})
```

---

## 验收标准

1. 所有新测试通过: `npx vitest run --project unit --project integration`
2. 性能基准可运行: `npx vitest bench`
3. Worker 测试覆盖 recoverStaleProjects 核心路径 + fatalShutdown
4. ReactContextManager 覆盖正常/压缩/边界场景
5. 4 个 Service 覆盖核心 CRUD + 异常路径
6. 4 个 Repository 覆盖 create/find/cleanup 操作
7. 5 个 Infrastructure 覆盖 event-bus/abort/auth/api-handler/pg-listener
8. 2 个 Domain 覆盖 risk-policy + scope-policy 规则
9. 新增约 67 个测试用例 + 9 个性能基准，零 skip

## 实施顺序建议

```
Task 1-2 (Worker + Context) → Task 3-6 (Services) → Task 7-10 (Repositories)
→ Task 11-15 (Infrastructure) → Task 16-17 (Domain) → Task 18-20 (性能基准)
```

Task 3-6 可并行；Task 7-10 可并行；Task 11-15 可并行；Task 18-20 可并行。
